# TODO

- ci providers configured in flake metadata
- default to 3 attestations
- support push to multiple registries
- move gh actions docker load/tag/push to be a flake app, gh action calls flake app. app
  is takes registry as argument. uses std env vars
